export enum NotificationStatus {
	SEEN = "yes",
	NOT_SEEN = "no",
}

export enum UserType {
	HOST_HUNTERS = "host_hunters",
	HOST = "host",
	GUESTS = "guests",
}

export enum UserReligion {
	CHRISTIANITY = "christianity",
	ISLAM = "islam",
	OTHERS = "others",
}

export enum UserActivationStatus {
	ONLINE = "online",
	OFFLINE = "offline",
	UNCOMPLETED = "uncompleted_setup_acct",
}
